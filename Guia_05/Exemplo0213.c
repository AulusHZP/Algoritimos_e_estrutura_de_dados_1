#include <stdio.h>

int main() {
  int num;
  printf("\nInforme o número: ");
  scanf ("%i",&num);
    if((15<num)&&(num<50)){
      printf("\nEsta dentro do intervalo");
        }else{
          printf("\N Não está dentro do intervalo");
            }
return 0;
}